# Retained V20 recording bytes

These eight JSONL files are unchanged copies of the root-qualified public CLI
requests and responses in phase28-physical-cpu-debug-actual-source-r2.
The source report and each byte pin are in provenance.json.

No compiler source authentication is encoded in these transport files. The site
only presents recorded observations; it does not run or admit a V20 kernel.
The one output allocation must not be relabeled as V21 read-only-input/output state.

The browser's mutation, empty-view and pagination tests are explicitly derived test
controls, not additional live recordings. The publication field is intentionally
null until a separately qualified compiler publication is supplied.
