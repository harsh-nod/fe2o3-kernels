# Positive source on a machine without the pinned production runtime
functional-refinement proof runtime unavailable at
/opt/fe2o3/verus-runtime-v2/functional-refinement-0.2026.08.02-b677dd5
compilation stopped before proof admission or artifact emission

# Mutate only the safe reference from 17 to 18
source-to-proof V2 effect mismatch: output argument 1 RHS mismatch between
reference block 0, statement 0 and GPU block 2, operation 0

# The mismatch is found before Verus runs. Other terminal joins include:
# - missing or extra GPU output effect
# - output argument mismatch
# - logical point coordinate mismatch
# - non-bounds GPU control, which this initial source adapter rejects
# - unsupported call, unsafe function, or ABI
# - multidimensional/raw read or unresolved ranked-load reconciliation
# - non-unit, eventful, noncanonical, or unproved loop termination

# Dynamic safe-slice bounds: an identical ranked extent is accepted.
# The Rust bounds assertion is retained and matched to the exact access, while
# the independent ranked relation proves point[0] < %arg3 over the full domain.
accepted: input_extent = %arg3, point_domain[0] = %arg3, index = point[0]

# An unrelated runtime length remains terminal Incomplete.
source-to-proof V2 cannot establish safe Rust slice bounds authority in reference block 0:
cannot prove full-domain bound `point[0]` < `%arg3` over point[0] < %arg4:
no exact ranked extent relation connects this bound to every point-coordinate extent

# Static affine bounds use an overflow-checked interval over the full domain.
accepted: input_extent = 16, point_domain[0] = 8, index = point[0] * 2 + 1
rejected: input_extent = 8, point_domain[0] = 8, index = point[0] + 1
derived index range 1..=8; the retained extent is unsafe or mismatched
rejected: index addition can overflow
rejected: safe-slice access `point[0]` has no exact retained bounds assertion

# Numerical sites are bound to exactly one complete logical output
error[FE2O3-PARALLEL-010]: finite error policy for relation 0 lacks a live typed witness or policy-checked staging record
error[FE2O3-PARALLEL-023]: numerical refinement site 0 does not match any logical output actual/reference roots
error[FE2O3-PARALLEL-024]: numerical refinement site 0 ambiguously matches 2 logical outputs
error[FE2O3-PARALLEL-025]: logical output 0 has more than one numerical refinement site
error[FE2O3-PARALLEL-026]: numerical refinement for logical output 0 is not total:
domain must be the canonical typed constant true

# Tensor receipts must bind every live tensor instruction to exactly one
# logical output and one bounded component map.
error[FE2O3-PARALLEL-013]: cooperative tensor functional refinement is incomplete: 1 live tensor site(s), but 0 exact result-component/output receipt binding(s)
error[FE2O3-PARALLEL-027]: tensor refinement site 0 does not match any logical output view and actual/reference roots
error[FE2O3-PARALLEL-028]: tensor refinement site 0 ambiguously matches 2 logical outputs
error[FE2O3-PARALLEL-029]: logical output 0 has more than one tensor refinement receipt
error[FE2O3-PARALLEL-030]: tensor refinement names non-live tensor instruction site ^bb2:0
error[FE2O3-PARALLEL-031]: tensor instruction site ^bb2:0 has more than one tensor refinement receipt

# Passing these claim-site joins is not formula-replay authority. The later
# per-compilation verifier stops unsupported ErrorBounded, tensor-component,
# fold, recurrence, permutation, and noncanonical-loop roles with
# UnsupportedFormulaReplayRole. That error is not a FE2O3-PARALLEL pass code.
# - unproved output separation, ownership collision, or duplicate write site

# Independent static bounds fixture
error[FE2O3-BOUNDS-001]: statically out-of-bounds Read at block 2 op 0;
access: v5 dimension 0;
required: 64 < 64
Rust source: .../src/lib.rs:65:20
ranked PLIRON before rejected lowering: kernel.index_constant 64
lowering stopped before target IR or artifact emission
